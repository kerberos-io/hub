export const STREAM_MODE_OPTIONS = {
  JPEG: "JPEG", 
  WEBRTC: "WEBRTC"
};

export const ZOOM_OPTIONS = Object.freeze({
    IN: 'in',
    OUT: 'out',
});

export const DIRECTIONS = Object.freeze({
    LEFT: 'left',
    RIGHT: 'right',
    UP: 'up',
    DOWN: 'down',
});